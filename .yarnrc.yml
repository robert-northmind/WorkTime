# Keep Git dependencies disallowed by leaving approvedGitRepositories absent.
# If a Git dependency is ever needed, discuss it explicitly before adding one.

enableGlobalCache: true

enableImmutableInstalls: true

enableScripts: false

nodeLinker: node-modules

npmMinimalAgeGate: 7d
